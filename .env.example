# ═══════════════════════════════════════════════════════════
# NOCTURN AI — Environment Configuration
# Copy this file to .env and fill in your values.
# ═══════════════════════════════════════════════════════════

# ─── Deployment Mode ──────────────────────────────────────
# Options: development | demo | production
# - development: local dev, mock channels, console logging
# - demo:        demo deployment for sales, real AI, simulated channels
# - production:  live deployment with real integrations
ENVIRONMENT=development

# ─── Database ─────────────────────────────────────────────
# PostgreSQL with pgvector extension required.
# Format: postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL=postgresql+asyncpg://sheerssoft:sheerssoft_dev_password@localhost:5433/sheerssoft

# Redis for sessions and real-time pub/sub.
REDIS_URL=redis://localhost:6380/0

# ─── AI / LLM (Required) ─────────────────────────────────
# OpenAI API Key — required for AI conversation engine.
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Model selection (defaults are fine for most cases)
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ─── AI Fallback (Optional) ──────────────────────────────
# Anthropic Claude Haiku — used automatically if OpenAI fails.
# Get yours at: https://console.anthropic.com/settings/keys
# Leave empty to disable fallback.
ANTHROPIC_API_KEY=

# ─── WhatsApp Business API ───────────────────────────────
# Required for live WhatsApp messaging.
# Setup guide: docs/setup/whatsapp.md
# Account: https://business.facebook.com → WhatsApp → API Setup
#
# Leave ALL empty to use mock mode (dev) or simulator (demo).
WHATSAPP_API_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_APP_SECRET=
WHATSAPP_VERIFY_TOKEN=sheerssoft_verify_token

# ─── Email / SendGrid ────────────────────────────────────
# Required for live email replies and daily reports.
# Setup guide: docs/setup/sendgrid.md
# Account: https://app.sendgrid.com/settings/api_keys
#
# Leave empty to use mock mode (dev) or simulator (demo).
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=concierge@yourdomain.com
STAFF_NOTIFICATION_EMAIL=reservations@yourhotel.com
SENDGRID_WEBHOOK_PUBLIC_KEY=

# ─── Authentication ──────────────────────────────────────
# JWT secret for dashboard authentication.
# IMPORTANT: Change this in production!
JWT_SECRET=dev_jwt_secret_change_in_production

# Admin credentials for dashboard login.
ADMIN_USER=admin
ADMIN_PASSWORD=change-this-in-production

# ─── Security / Encryption ──────────────────────────────
# Fernet key for PII field-level encryption (PDPA compliance).
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Leave empty to disable PII encryption (dev only).
FERNET_ENCRYPTION_KEY=

# ─── Frontend ────────────────────────────────────────────
# Dashboard URL (used for CORS)
ALLOWED_ORIGINS=http://localhost:3000
